<h1>Listing counters</h1>

<table>
  <thead>
    <tr>
      <th>Fullname</th>
      <th>Currency</th>
      <th>Listedboard</th>
      <th>Sgxcode</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @counters.each do |counter| %>
      <tr>
        <td><%= counter.fullname %></td>
        <td><%= counter.currency %></td>
        <td><%= counter.listedBoard %></td>
        <td><%= counter.sgxcode %></td>
        <td><%= link_to 'Show', counter %></td>
        <td><%= link_to 'Edit', edit_counter_path(counter) %></td>
        <td><%= link_to 'Destroy', counter, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Counter', new_counter_path %>
